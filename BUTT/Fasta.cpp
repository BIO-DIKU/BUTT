#include "Fasta.h"

#include <vector>

Fasta::Fasta(std::string filePath) {
    std::vector<int> v;
    std::vector<int>::iterator it;
    for(it = v.begin(); it!=v.end(); it++){
        //Do something
        int value = *it;
    }
}
